import React from 'react';
import "./Chat.css"

const Chat = ({name,message,timestamp,profilePic}) => {
    return (
        <div  className="chat">
            
        </div>
    );
};

export default Chat;